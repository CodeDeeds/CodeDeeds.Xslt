<?xml version="1.0" encoding="utf-8"?>
<!--
  The schema for Data/products.xml, used by the schema-aware benchmarks.

  Typed enough to be worth validating against: an integer id, a decimal price with a currency code of a
  restricted type, a boolean, a rating held to a range, and a name that is an ID. Validating a document
  against a schema of one xs:anyType would measure the walk and not the checking.
-->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">

  <xs:simpleType name="currencyCode">
    <xs:restriction base="xs:string">
      <xs:pattern value="[A-Z]{3}"/>
    </xs:restriction>
  </xs:simpleType>

  <xs:simpleType name="rating">
    <xs:restriction base="xs:decimal">
      <xs:minInclusive value="0"/>
      <xs:maxInclusive value="5"/>
    </xs:restriction>
  </xs:simpleType>

  <xs:complexType name="money">
    <xs:simpleContent>
      <xs:extension base="xs:decimal">
        <xs:attribute name="currency" type="currencyCode" use="required"/>
      </xs:extension>
    </xs:simpleContent>
  </xs:complexType>

  <xs:element name="products">
    <xs:complexType>
      <xs:sequence>
        <xs:element name="product" maxOccurs="unbounded">
          <xs:complexType>
            <xs:sequence>
              <xs:element name="id" type="xs:integer"/>
              <xs:element name="name" type="xs:string"/>
              <xs:element name="category" type="xs:string"/>
              <xs:element name="price" type="money"/>
              <xs:element name="description" type="xs:string"/>
              <xs:element name="inStock" type="xs:boolean"/>
              <xs:element name="rating" type="rating"/>
            </xs:sequence>
          </xs:complexType>
        </xs:element>
      </xs:sequence>
    </xs:complexType>
  </xs:element>

</xs:schema>
